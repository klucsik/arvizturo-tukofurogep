from app.models import *

from app import storekeeper, db
from flask_admin.contrib.sqla import ModelView

'''
veiws
'''




'''
storekeeper interface
'''
#
# storekeeper.add_view(ModelView(User, db.session))
# storekeeper.add_view(ModelView(Stores, db.session))
# storekeeper.add_view(ModelView(Product, db.session))
# storekeeper.add_view(ModelView(UseCategory, db.session))
#
# storekeeper.add_view(ModelView(Charity, db.session))
# storekeeper.add_view(ModelView(Chain, db.session))
# storekeeper.add_view(ModelView(ProductCategory, db.session))
# storekeeper.add_view(ModelView(Cart, db.session))
# storekeeper.add_view(ModelView(ConnectCartProduct, db.session))
# storekeeper.add_view(ModelView(HandlingCategory, db.session))